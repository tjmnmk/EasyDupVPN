{
  "TUN_ADDRESS_IPV4": "10.0.0.1",
  "TUN_NETMASK_IPV4": 24,
  "TUN_ADDRESS_IPV6": "fd00::1",
  "TUN_NETMASK_IPV6": 64,
  "PEER_ADDRESS": "192.168.1.133",
  "_comment_PEER_ADDRESS": "For peer behind >>> NAT <<< or with dynamic IP, see PEER_LEARN setting",
  "PEER_PORT": 8111,
  "PEER_LEARN": "off",
  "_comment_PEER_LEARN": ["Set to \"learn\" if the peer address is dynamic or client is behind >>> NAT <<<",
                          "in which case the peer address is learned from first incoming packets",
                          "Set to \"dynamic\" to allow relearning the peer address from each incoming packet",
                          "Set to \"off\" to disable learning/relearning of peer address",
                          "",
                          "If set to \"learn\" or \"dynamic\", the PEER_ADDRESS and PEER_PORT settings are ignored."],
  "LISTEN_ADDRESS": "0.0.0.0",
  "TUN_DEVICE_NAME": "tun-edv0",
  "MTU": 1370,
  "_comment_MTU": ["Recommended to set 130 bytes lower than the MTU of the underlying network (e.g., for standard Ethernet 1500 use 1370)",
                   "If you need MTU == 1500, set VPN_DATA_MAX_SIZE_SPLIT to 1000 for example"],
  "VPN_DATA_MAX_SIZE_SPLIT": 0,
  "_comment_VPN_DATA_MAX_SIZE_SPLIT": ["Maximum size of Data part in each split packet.",
                                      "Set to 0 to disable packet splitting.",
                                      "If you experience issues with MTU, set this to a value of around 2/3 of your underlying network MTU.",
                                      "If is set then it must be set on both peers but can be different values."],
  "ENCRYPTION_KEY": "CHANGEME",
  "_comment_ENCRYPTION_KEY": "Use easydupvpn-genkey to generate a secure random key",
  "DEDUPLICATION_TTL_SECONDS": 60,
  "_comment_DEDUPLICATION_TTL_SECONDS": "Time in seconds to remember seen nonces for deduplication purposes",
  "NUMBER_OF_DUPLICATES": 3,
  "_comment_NUMBER_OF_DUPLICATES": "Number of duplicate packets to send for each original packet",
  "SEND_EXTRA_DELAYED_PACKET_AFTER": 0.05,
  "_comment_SEND_EXTRA_DELAYED_PACKET_AFTER": [ "if not 0 then send extra duplicate packet after specified delay in seconds (can help with lossy networks).",
                                                "Note: timing is not guaranteed, actual delay may be longer (up to several hundred ms in extreme cases with no traffic)"],
  "LISTEN_PORT": 8111,
  "LOG_LEVEL": "INFO",
  "NICE_LEVEL": -10,
  "PROTOCOL_HEADER": true,
  "_comment_PROTOCOL_HEADER": "Adds 'RUV' magic bytes to packets. Set to false to make VPN traffic harder to identify (both peers must use the same setting)",
  "COMPRESSION": false,
  "_comment_COMPRESSION": ["Set to true to enable compression (both peers must use the same setting)",
                          "Compression can improve throughput on slow or congested networks,",
                          "but increases CPU usage and adds latency.",
                          "Zlib compression is used - level 6." ],
  "ADD_ROUTES": [],
  "_comment_ADD_ROUTES": ["List of additional routes to add when the VPN is started.",
                         "Each route is specified as a string in CIDR notation, e.g., \"192.168.1.0/24\" or \"fd00::/64\".",
                         "Don't add 0.0.0.0/0 or ::/0 here (it can block traffic to the peer) - use DEFAULT_ROUTE setting instead."],
  "DEFAULT_ROUTE": false,
  "_comment_DEFAULT_ROUTE": "Set to true to route all traffic through the VPN",
  "ADD_ROUTES_PEER_IPV4": "10.0.0.2",
  "_comment_ADD_ROUTES_PEER_IPV4": "When using ADD_ROUTES, set this to the IPv4 TUN address of the peer",
  "ADD_ROUTES_PEER_IPV6": "fd00::2",
  "_comment_ADD_ROUTES_PEER_IPV6": "When using ADD_ROUTES, set this to the IPv6 TUN address of the peer",
  "RUN_COMMAND_AFTER_TUN_READY": "",
  "_comment_RUN_COMMAND_AFTER_TUN_READY": [ "Command to run after TUN device is ready (e.g., to change firewall, or resolv.conf)",
                                            "Use with CAUTION - command is run as root."],
  "RUN_COMMAND_ON_EXIT": "",
  "_comment_RUN_COMMAND_ON_EXIT": [ "Command to run on exit (e.g., to restore firewall, or resolv.conf)",
                                   "Use with CAUTION - command is run as root."]
}
